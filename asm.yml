---
env:
  KEY: value
  ENV: staging

steps:
  - name: test
    cmd: env

  - name: test
    cmd: false

  - name: uname
    cmd: uname -a

deploy:
  - provider: s3
    access_key_id: "YOUR AWS ACCESS KEY"
    secret_access_key: "YOUR AWS SECRET KEY"
    bucket: "S3 Bucket"
    glob: "*.txt"
  - provider: s3
    access_key_id: "YOUR AWS ACCESS KEY"
    secret_access_key: "YOUR AWS SECRET KEY"
    bucket: "Second S3 Bucket"
    src: file
